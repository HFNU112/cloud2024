spring:
  application:
    name: payment-service
  cloud:
    consul:
      host: localhost  # consul地址
      port: 8500       # consul端口 默认：8500
      discovery:
        service-name: ${spring.application.name}  # 注册服务名称
    config:
      enabled: true
      profileSeparator: '-' # default value is ","，we update '-'
      format: YAML
#config/payment-service/data
#      /payment-service-dev/data
#      /payment-service-prod/data